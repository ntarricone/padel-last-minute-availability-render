services:
  - type: web
    name: padel-last-minute-availability
    runtime: node
    plan: free
    buildCommand: npm install && npm test
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: true
